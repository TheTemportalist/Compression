package com.temportalist.compression.client.integration.jei;

import com.temportalist.compression.common.recipes.RecipeCompress;

public class RecipeWrapperCompress extends RecipeWrapper<RecipeCompress>
{
    public RecipeWrapperCompress(RecipeCompress recipe)
    {
        super(recipe);
    }
}
